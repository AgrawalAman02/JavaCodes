package Linked_List;
//find the maximum twin sum of a lnked list of even length
// twin of ant node at ith index is the node at n-i-1 th index .twin sum is the sum of node and its twin
public class practice_11_twin_sum {
    static class Node{
        int data;
        Node next;
        Node(int data ){
            this.data = data;
        }
    }

    public static void main(String[] args) {
//      do the same way as u do in palindrome by slow and fast ptr
//        until u reverse the next halve and then declare p2 to start and temp to slow.next
//        now declare a min value by Integer.MIN value
//         now dtart a loop whwre  u add the element of the fisrt halve and then compare it with the max vale then got the max value
//        / so do in this way

    }
}
